<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>bin</id>
    <formats>
        <format>tar.gz</format>
        <format>zip</format>
    </formats>

    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>simple-pic-${project.version}</baseDirectory>

    <fileSets>
        <!-- JAR file -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>

        <!-- External application.yml (user can edit port, logging, etc.) -->
        <fileSet>
            <directory>${project.build.directory}/classes</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>application.yml</include>
            </includes>
        </fileSet>

        <!-- Management script for Linux/macOS -->
        <fileSet>
            <directory>src/assembly/scripts</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>simple-pic.sh</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Management script for Windows -->
        <fileSet>
            <directory>src/assembly/scripts</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>simple-pic.bat</include>
            </includes>
        </fileSet>

        <!-- README for distribution -->
        <fileSet>
            <directory>src/assembly</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>README.md</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>
